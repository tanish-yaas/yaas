/**
 * The sentence the delete dialog asks for, alongside the workspace's name.
 * Shared by the dialog and the action so the server checks exactly what the
 * user was asked to type — the dialog's check is a courtesy, not the gate.
 */
export const DELETE_WORKSPACE_PHRASE = "delete my workspace";
export const DELETE_ACCOUNT_PHRASE = "delete my account";
